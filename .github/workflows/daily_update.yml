name: top 10k players

on:
  schedule:
    # تشغيل كل يوم الساعة 5:00 صباحاً بتوقيت جرينتش
    - cron: '0 5 * * *'
  workflow_dispatch: # يسمح لك بتشغيله يدوياً أيضاً إذا أردت

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run FPL Script
        run: python top_10k_players.py # استبدل 'top_10k_players.py' باسم ملف الكود الخاص بك

      - name: Commit and Push changes
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add top_10k_data.json top_10k_data.db
          git commit -m "Update FPL Elite Data - $(date)" || echo "No changes to commit"
          git push
